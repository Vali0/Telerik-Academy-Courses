using System;

class Task01WhatIsString
{
    //Describe the strings in C#. What is typical for the string data type? 
    //Describe the most important methods of the String class.
    static void Main(string[] args)
    {
        /*
         * Strings are collections of characters stored on specific addres of the memory
         * This type allows us to manipulate character data through different methods and properties.
         * We can use string methods to concatinate, split, replace, remove, substring, trim, comparing, search, etc.
         * 
         * The most important methods are:
         * Read(), ReadLine(), Write(), WriteLine(), Equals(), Compare(), Contains(), Concat(), ToUpper(), ToLower(), IndexOf(),
         * Substring(), Replace(), Remove(), Trim(), Split(), Format(), Parse(), ToString(), PadLeft()/Right(), Length; 
         */
    }
}